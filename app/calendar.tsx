import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Calendar = () => {
	return (
		<View>
			<Text>Add calendar in here</Text>
		</View>
	);
};

export default Calendar;

const styles = StyleSheet.create({});
